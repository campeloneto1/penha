import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    backgroundColor: "#fef",
    minHeight: "100%",
    minWidth: "100%",
  },
});

export default styles;
